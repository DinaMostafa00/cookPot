<h1>projects</h1>

{{#if projects.length}}
  <ul>
    {{#each projects}}
      <li>
        <a href="/projects/{{id}}">
          {{title}}</a>

      </li>
    {{/each}}
  </ul>

{{else}}

  <p> wait for upcoming projects</p>

{{/if}}