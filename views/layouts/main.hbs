<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dina's portfolio</title>
    <link rel="stylesheet" href="/spectre.min.css" />
    <link rel="stylesheet" href="/spectre-exp.min.css" />
    <link rel="stylesheet" href="/spectre-icons.min.css" />
    <link rel="stylesheet" href="/layout.css" />
  </head>
  <body>
    <div class="headerAdjusting">
      <header class="navbar">
        <section class="navbar-section">
          <a href="/" class="navbar-brand mr-2">Home</a>
          <a href="/recipes" class="btn btn-link">Recipes</a>

          {{#if isLoggedIn}}
            <a href="/createRecipe" class="btn btn-link">Create Recipe</a>
          {{/if}}

          <a href="/blogPosts" class="btn btn-link">Blogs</a>

          {{#if isLoggedIn}}
            <a href="/createBlogPost" class="btn btn-link">Create Blog Post</a>
          {{/if}}

          <a href="/aboutme" class="btn btn-link">About Me</a>

          <a href="/contactme" class="btn btn-link">Contact Me</a>

          {{#unless isLoggedIn}}
            <a href="/login" class="btn btn-link">Login</a>
          {{/unless}}

          {{#if isLoggedIn}}
            <a href="/logout" class="btn btn-link">Logout</a>
          {{/if}}
        </section>

        <section class="navbar-section">
          <div class="input-group input-inline">
            <button class="btn btn-primary input-group-btn">
              <a href="/search"> Search</a>
            </button>
          </div>
        </section>
      </header>
    </div>

    {{!-- <a href="/">Home</a> 
            
             <a href="/recipes">Recipes</a></li>

              {{#if isLoggedIn}}
              <a href="/createRecipe"> Create Recipe</a> 
              {{/if}}

            <a href="/blogs">Blogs</a>

              {{#if isLoggedIn}}
             <a href="/createBlogPost"> Create Blog Post</a>
              {{/if}}

          
            <a href="/aboutme">About Me</a>

       
       
          <a href="/contactme">Contact Me</a>

          {{#unless isLoggedIn}}
          <a href="/login">Login</a>
          {{/unless}}

          {{#if isLoggedIn}}
       <a href="/logout">Logout</a>
          {{/if}} --}}

    {{{body}}}

    <footer>
      Copyright Dina L-G 2022
    </footer>

  </body>
</html>